[
  {
    "id": "TC001",
    "title": "health check api returns server status and timestamp",
    "description": "Test the GET /api/health endpoint to verify it returns a 200 status with a JSON object containing status, message, and timestamp fields when the server is running normally."
  },
  {
    "id": "TC002",
    "title": "authentication login returns token and user on valid credentials",
    "description": "Test the POST /api/auth/login endpoint with valid username and password to ensure it returns a 200 status with a JWT token and user object containing id, username, fullName, and role."
  },
  {
    "id": "TC003",
    "title": "authentication me returns user for valid token",
    "description": "Test the GET /api/auth/me endpoint with a valid Authorization Bearer token to verify it returns a 200 status with the authenticated user object."
  },
  {
    "id": "TC004",
    "title": "authentication logout acknowledges logout with message",
    "description": "Test the POST /api/auth/logout endpoint with a valid Authorization Bearer token to ensure it returns a 200 status with a message acknowledging logout."
  },
  {
    "id": "TC005",
    "title": "user management list users requires admin token",
    "description": "Test the GET /api/users endpoint with an admin Authorization Bearer token to verify it returns a 200 status with a list of users excluding password hashes."
  },
  {
    "id": "TC006",
    "title": "user management create user with admin token",
    "description": "Test the POST /api/users endpoint with an admin Authorization Bearer token and valid user data to ensure it creates a new user and returns a 201 status with the created user object."
  },
  {
    "id": "TC007",
    "title": "user management update user fields with admin token",
    "description": "Test the PUT /api/users/:id endpoint with an admin Authorization Bearer token and valid update fields (fullName, role, password) to verify it updates the user and returns a 200 status with the updated user object."
  },
  {
    "id": "TC008",
    "title": "user management change user status with admin token",
    "description": "Test the PATCH /api/users/:id/status endpoint with an admin Authorization Bearer token and a body containing isActive boolean to verify it activates or deactivates the user account, returning a 200 status with the updated user."
  }
]
